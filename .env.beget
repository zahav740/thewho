# =================================================================
# КОНФИГУРАЦИЯ ДЛЯ BEGET - ОБНОВИТЕ ЗНАЧЕНИЯ ПЕРЕД РАЗВЕРТЫВАНИЕМ!
# =================================================================

# 🔐 БЕЗОПАСНОСТЬ JWT
# Сгенерируйте случайный ключ: https://generate-secret.vercel.app/64
JWT_SECRET=aB3$kL9#mP2@wR7*qX5!nY8&zC1^vF4+dE6%hI0)jK3&lM7^nO2*pQ5#rS8!tU1@
JWT_EXPIRES_IN=7d

# 🗄️ БАЗА ДАННЫХ
# Используйте данные из панели Beget → "Базы данных" → "MySQL/PostgreSQL"
DB_HOST=postgres
DB_PORT=5432
DB_NAME=thewho_prod
DB_USERNAME=postgres
# Создайте безопасный пароль для БД (минимум 12 символов)
DB_PASSWORD=MySecureDB_Password123!

# ⚙️ КОНФИГУРАЦИЯ API
NODE_ENV=production
PORT=3001

# 🌐 ДОМЕН И CORS
# ВЫБЕРИТЕ ОДИН ИЗ ВАРИАНТОВ:

# Вариант 1: Если у вас есть домен kasuf.xyz
CORS_ORIGIN=https://kasuf.xyz
REACT_APP_API_URL=https://kasuf.xyz/api
DOMAIN=kasuf.xyz

# Вариант 2: Если используете поддомен Beget (раскомментируйте строки ниже)
# CORS_ORIGIN=https://kasuf.beget.tech
# REACT_APP_API_URL=https://kasuf.beget.tech/api
# DOMAIN=kasuf.beget.tech

# Вариант 3: Если создаете поддомен (раскомментируйте строки ниже)
# CORS_ORIGIN=https://crm.kasuf.xyz
# REACT_APP_API_URL=https://crm.kasuf.xyz/api
# DOMAIN=crm.kasuf.xyz

# 📧 НАСТРОЙКИ
EMAIL=kasuf19@gmail.com

# 🔧 ДОПОЛНИТЕЛЬНЫЕ НАСТРОЙКИ
REACT_APP_ENVIRONMENT=production
MAX_FILE_SIZE=50MB
UPLOAD_PATH=/app/uploads
LOG_LEVEL=info

# 📝 ИНСТРУКЦИИ:
# 1. Сгенерируйте новый JWT_SECRET
# 2. Установите безопасный пароль для DB_PASSWORD
# 3. Выберите и раскомментируйте нужный вариант домена
# 4. Сохраните файл
