/**\n * Тестовый скрипт для проверки синхронизации планирования со сменами\n * Запустить в консоли браузера для диагностики проблем\n */\n\n// Функция для проверки состояния синхронизации\nfunction checkSyncStatus() {\n  console.log('🔍 Проверка состояния синхронизации...');\n  \n  // Проверяем настройки\n  const autoSyncEnabled = localStorage.getItem('autoSyncEnabled');\n  const lastSyncTime = localStorage.getItem('lastSyncTime');\n  const lastSyncError = localStorage.getItem('lastSyncError');\n  \n  console.log('⚙️ Настройки:');\n  console.log('  - Автосинхронизация:', autoSyncEnabled === 'true' ? '✅ Включена' : '❌ Отключена');\n  console.log('  - Последняя синхронизация:', lastSyncTime ? new Date(lastSyncTime).toLocaleString() : 'Никогда');\n  console.log('  - Последняя ошибка:', lastSyncError || 'Нет');\n  \n  return {\n    autoSyncEnabled: autoSyncEnabled === 'true',\n    lastSyncTime,\n    lastSyncError\n  };\n}\n\n// Функция для проверки данных в localStorage\nfunction checkLocalStorageData() {\n  console.log('\\n📦 Проверка данных в localStorage...');\n  \n  const orders = JSON.parse(localStorage.getItem('orders') || '[]');\n  const shifts = JSON.parse(localStorage.getItem('shifts') || '[]');\n  const planningResults = JSON.parse(localStorage.getItem('planningResults') || '[]');\n  \n  console.log('📋 Заказы:', orders.length);\n  console.log('🔄 Смены:', shifts.length);\n  console.log('📅 Результаты планирования:', planningResults.length);\n  \n  // Анализируем операции\n  const totalOperations = orders.reduce((sum, order) => sum + (order.operations?.length || 0), 0);\n  console.log('🔧 Всего операций в заказах:', totalOperations);\n  \n  // Анализируем операции в сменах\n  const shiftOperations = shifts.reduce((sum, shift) => sum + (shift.operations?.length || 0), 0);\n  console.log('🏭 Операций в сменах:', shiftOperations);\n  \n  return {\n    orders: orders.length,\n    shifts: shifts.length,\n    planningResults: planningResults.length,\n    totalOperations,\n    shiftOperations\n  };\n}\n\n// Функция для проверки соответствия операций в сменах и заказах\nfunction checkOperationMatching() {\n  console.log('\\n🔍 Проверка соответствия операций...');\n  \n  const orders = JSON.parse(localStorage.getItem('orders') || '[]');\n  const shifts = JSON.parse(localStorage.getItem('shifts') || '[]');\n  \n  const matches = [];\n  const mismatches = [];\n  \n  for (const shift of shifts) {\n    if (!shift.operations) continue;\n    \n    for (const shiftOp of shift.operations) {\n      let found = false;\n      \n      // Ищем по ID операции\n      if (shiftOp.operationId) {\n        for (const order of orders) {\n          const operation = order.operations?.find(op => op.id === shiftOp.operationId);\n          if (operation) {\n            matches.push({\n              drawingNumber: shiftOp.drawingNumber,\n              operationId: shiftOp.operationId,\n              matchType: 'by_id',\n              orderDrawing: order.drawingNumber,\n              operationSequence: operation.sequenceNumber\n            });\n            found = true;\n            break;\n          }\n        }\n      }\n      \n      // Если не нашли по ID, ищем по номеру чертежа\n      if (!found && shiftOp.drawingNumber) {\n        const order = orders.find(o => o.drawingNumber === shiftOp.drawingNumber);\n        if (order && order.operations?.length > 0) {\n          matches.push({\n            drawingNumber: shiftOp.drawingNumber,\n            operationId: shiftOp.operationId,\n            matchType: 'by_drawing',\n            orderDrawing: order.drawingNumber,\n            operationSequence: order.operations[0].sequenceNumber\n          });\n          found = true;\n        }\n      }\n      \n      if (!found) {\n        mismatches.push({\n          drawingNumber: shiftOp.drawingNumber,\n          operationId: shiftOp.operationId,\n          machine: shift.machine,\n          date: shift.date\n        });\n      }\n    }\n  }\n  \n  console.log('✅ Найдено соответствий:', matches.length);\n  console.log('❌ Не найдено соответствий:', mismatches.length);\n  \n  if (matches.length > 0) {\n    console.log('\\n📊 Примеры найденных соответствий:');\n    matches.slice(0, 5).forEach((match, i) => {\n      console.log(`  ${i + 1}. ${match.drawingNumber} (${match.matchType}) → ${match.orderDrawing}:${match.operationSequence}`);\n    });\n  }\n  \n  if (mismatches.length > 0) {\n    console.log('\\n⚠️ Операции без соответствий:');\n    mismatches.forEach((mismatch, i) => {\n      console.log(`  ${i + 1}. ${mismatch.drawingNumber} (ID: ${mismatch.operationId}) на ${mismatch.machine}`);\n    });\n  }\n  \n  return { matches, mismatches };\n}\n\n// Функция для анализа смен станков\nfunction analyzeMachineChanges() {\n  console.log('\\n🔄 Анализ смен станков...');\n  \n  const shifts = JSON.parse(localStorage.getItem('shifts') || '[]');\n  const planningResults = JSON.parse(localStorage.getItem('planningResults') || '[]');\n  \n  const machineChanges = [];\n  \n  for (const shift of shifts) {\n    if (!shift.operations) continue;\n    \n    for (const shiftOp of shift.operations) {\n      const planning = planningResults.find(pr => pr.operationId === shiftOp.operationId);\n      \n      if (planning && planning.machine !== shift.machine) {\n        machineChanges.push({\n          operationId: shiftOp.operationId,\n          drawingNumber: shiftOp.drawingNumber,\n          plannedMachine: planning.machine,\n          actualMachine: shift.machine,\n          isRescheduled: planning.status === 'rescheduled'\n        });\n      }\n    }\n  }\n  \n  console.log('🔄 Обнаружено смен станков:', machineChanges.length);\n  \n  if (machineChanges.length > 0) {\n    console.log('\\n📋 Детали смен станков:');\n    machineChanges.forEach((change, i) => {\n      const status = change.isRescheduled ? '✅ Обновлено' : '❌ НЕ обновлено';\n      console.log(`  ${i + 1}. ${change.drawingNumber}: ${change.plannedMachine} → ${change.actualMachine} (${status})`);\n    });\n  }\n  \n  return machineChanges;\n}\n\n// Главная функция диагностики\nfunction runSyncDiagnostics() {\n  console.clear();\n  console.log('🚀 Запуск диагностики синхронизации планирования...');\n  console.log('================================================\\n');\n  \n  const syncStatus = checkSyncStatus();\n  const dataStatus = checkLocalStorageData();\n  const operationMatching = checkOperationMatching();\n  const machineChanges = analyzeMachineChanges();\n  \n  console.log('\\n📊 СВОДКА ДИАГНОСТИКИ:');\n  console.log('=====================');\n  console.log('✅ Автосинхронизация:', syncStatus.autoSyncEnabled ? 'Включена' : 'Отключена');\n  console.log('📋 Всего заказов:', dataStatus.orders);\n  console.log('🔄 Всего смен:', dataStatus.shifts);\n  console.log('🔧 Операций в заказах:', dataStatus.totalOperations);\n  console.log('🏭 Операций в сменах:', dataStatus.shiftOperations);\n  console.log('✅ Соответствий найдено:', operationMatching.matches.length);\n  console.log('❌ Несоответствий:', operationMatching.mismatches.length);\n  console.log('🔄 Смен станков:', machineChanges.length);\n  \n  // Рекомендации\n  console.log('\\n💡 РЕКОМЕНДАЦИИ:');\n  console.log('===============');\n  \n  if (!syncStatus.autoSyncEnabled) {\n    console.log('❗ Включите автосинхронизацию в настройках');\n  }\n  \n  if (operationMatching.mismatches.length > 0) {\n    console.log('❗ Проверьте правильность ID операций и номеров чертежей в сменах');\n  }\n  \n  if (machineChanges.length > 0) {\n    const notUpdated = machineChanges.filter(c => !c.isRescheduled).length;\n    if (notUpdated > 0) {\n      console.log(`❗ ${notUpdated} смен станков не отражены в планировании - запустите синхронизацию`);\n    }\n  }\n  \n  if (dataStatus.shiftOperations === 0) {\n    console.log('❗ В сменах нет операций - добавьте данные о выполненных работах');\n  }\n  \n  console.log('\\n✅ Диагностика завершена!');\n  \n  return {\n    syncStatus,\n    dataStatus,\n    operationMatching,\n    machineChanges\n  };\n}\n\n// Функция для принудительной синхронизации\nfunction forceSyncWithPlanning() {\n  console.log('🚀 Принудительная синхронизация...');\n  \n  // Импортируем сервис синхронизации (если доступен)\n  if (typeof window !== 'undefined' && window.shiftPlanningSync) {\n    return window.shiftPlanningSync.syncShiftsWithPlanning();\n  } else {\n    console.log('❌ Сервис синхронизации недоступен. Используйте кнопку в интерфейсе.');\n    return Promise.reject('Sync service not available');\n  }\n}\n\n// Экспортируем функции в глобальную область видимости\nif (typeof window !== 'undefined') {\n  window.syncDiagnostics = {\n    run: runSyncDiagnostics,\n    checkStatus: checkSyncStatus,\n    checkData: checkLocalStorageData,\n    checkMatching: checkOperationMatching,\n    analyzeMachineChanges: analyzeMachineChanges,\n    forceSync: forceSyncWithPlanning\n  };\n  \n  console.log('\\n🔧 Функции диагностики доступны в консоли:');\n  console.log('- syncDiagnostics.run() - полная диагностика');\n  console.log('- syncDiagnostics.checkStatus() - проверка настроек');\n  console.log('- syncDiagnostics.checkData() - проверка данных');\n  console.log('- syncDiagnostics.checkMatching() - проверка соответствий');\n  console.log('- syncDiagnostics.analyzeMachineChanges() - анализ смен станков');\n  console.log('\\n▶️ Запустите syncDiagnostics.run() для начала диагностики');\n}\n\n// Автоматический запуск при загрузке\nif (typeof window !== 'undefined' && document.readyState === 'complete') {\n  setTimeout(() => {\n    console.log('\\n🔄 Автоматический запуск диагностики через 2 секунды...');\n    console.log('Для отмены выполните: clearTimeout(window.autoDiagnosticTimeout)');\n    \n    window.autoDiagnosticTimeout = setTimeout(runSyncDiagnostics, 2000);\n  }, 1000);\n}\n